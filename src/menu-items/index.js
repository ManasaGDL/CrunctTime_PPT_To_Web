import dashboard from './dashboard';
import pages from './pages';
import admin from './admin';
import other from './other';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, pages, admin, other]
};

export default menuItems;
